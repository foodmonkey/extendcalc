//  ui keypads list - this is the user interface basically
//  list of keys in the businesskeypad library (directory)

Keypads(
    keypads: [
        KeyRef("business",),
        KeyRef("conversion",),
        KeyRef("scientific",),
    ]
)
